[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "gs2d-video"
version = "0.1.0"
description = "2D Gaussian Splatting temporal para video con rasterizador CUDA tiled"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
